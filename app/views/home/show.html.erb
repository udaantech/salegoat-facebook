%h2 Order

%p
  %label Amount
  = @order.amount
%p
  %label Description
  = @order.description

%p
  %label Payment
  - if @order.payment.present?
    - if @order.payment.state == "approved"
      Payed with
      = @order.payment.payer.payment_method
    - elsif @order.approve_url
      = link_to "Pay with PayPal", @order.approve_url
    - else
      Payment or completed(#{@order.payment.state})

